from distutils.core import setup
setup(
  name = 'hashwrapper',
  packages = ['hashwrapper'], # this must be the same as the name above
  version = '0.51',
  description = 'hashwrapper - wrapper around hashing methods [hashlib]',
  author = 'Gaurab B',
  author_email = 'gaurabb@hotmail.com',
  url = 'https://github.com/gaurabb/hashwrapper', # use the URL to the github repo
  download_url = 'https://github.com/gaurabb/hashwrapper/tarball/0.5', # I'll explain this in a second
  keywords = ['hash', 'salt' 'crypto', 'hashlib'], # arbitrary keywords
  classifiers = [],
)
